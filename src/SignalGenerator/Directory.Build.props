<Project>
  <Import Project="../../Directory.Build.props"/>
  <PropertyGroup Label="Configuration">
    <!-- Nasty hack to help with debugging auto configuration -->
    <TargetExt>.dll</TargetExt>
    <TargetPath>$(OutDir)$(TargetName)$(TargetExt)</TargetPath>
  </PropertyGroup>
</Project>